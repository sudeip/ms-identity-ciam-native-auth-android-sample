# AI Agent Instructions

## Git workflow

- **Never commit and push code automatically.** Only run `git commit` or `git push` when the user explicitly asks for it. Make local changes only and leave committing/pushing to the user unless instructed otherwise.
